import com.pkisi.kucd.MapstructInterface;
import com.pkisi.kucd.Users;
import com.pkisi.kucd.UsersDTO;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.mapstruct.factory.Mappers;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.junit4.SpringRunner;

//@RunWith(SpringRunner.class)
public class DtoTest {
    /*@Autowired
    MapstructInterface mapstructInterface;
    //private MapstructInterface mapstructInterface = Mappers.getMapper(MapstructInterface.class);
    @Test
    public void givenDtoTest(){*/

        /*Users users = new Users();
        users.setId(1);
        users.setLogin("kek");
        users.setNickname("nie");
        users.setPassword("post");
        UsersDTO dto = MapstructInterface.INSTANCE.usersToUsersDTO(users);
        Assert.assertNotNull(dto.getUser_id());*/

}
